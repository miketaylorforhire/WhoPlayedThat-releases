# Screenshots

Place app screenshots here, referenced by `../README.md`:

| File | What to capture |
| --- | --- |
| `hero.png` | Main library view — album grid with the LetterNav strip visible, ideally with a track playing in the bar |
| `album-detail.png` | Album detail page — sticky cover on the left, contributing-artist chips, track list with mic icons |
| `artist-collaborators.png` | Artist page on the **Collaborators** tab, ideally drilled into a shared-tracks view (e.g. `Ike Quebec × Sonny Clark`) |
| `mini-player.png` | Mini player window — compact size, hovered so the transport controls are visible |

PNG, 16:10 or 16:9 aspect, ~1600px wide is plenty. Crop tight to the app window (no surrounding desktop).
